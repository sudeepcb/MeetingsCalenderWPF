<Window x:Class="MeetingsCalenderWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MeetingsCalenderWPF"
        xmlns:views="clr-namespace:MeetingsCalenderWPF.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <!-- Main grid layout -->
    <Grid>
        <!-- Define a single row -->
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Include the LoginView UserControl -->
        <views:LoginView x:Name="loginView" Grid.Row="0" Margin="0,-70,-68,10"/>
        <!-- Use a Frame to display different views -->
        <Frame x:Name="EventFrame"></Frame>
    </Grid>
</Window>
